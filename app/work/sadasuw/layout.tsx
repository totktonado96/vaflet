import type { ReactNode } from "react";
import "./sadasuw.css";

/* The case wears the plant's own type. The files sit on this site, the same
   way the plant's site keeps its fonts at home — and only this route loads them. */

export default function SadaSuwLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
